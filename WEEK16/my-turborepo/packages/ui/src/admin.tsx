//adding the admin page -- and need to update the export of the component in packeage.json >>> doing now mannualy 
//turbo can alos it ot itself >> well we will see it 
// >>>> go to the package/ui folder and runt the command npm run generate:component

export default function Admin(){
    return (
        <div>Admin Page</div>
    )
}